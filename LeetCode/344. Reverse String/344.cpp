class Solution {
public:
    void reverseString(vector<char>& s) {
        reverse(s.begin(), s.end());  // Call the reverse function from the algorithm library to reverse the elements of the vector in-place
    }
};